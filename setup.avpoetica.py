import setuptools

# Most of the package metadata (name, version, description, dependencies, etc.)
# is defined in the pyproject.toml file.
# setuptools will automatically read the configuration from pyproject.toml
# when you run build or install commands (e.g., pip install .).

if __name__ == "__main__":
    setuptools.setup()
